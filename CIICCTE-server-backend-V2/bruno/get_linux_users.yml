info:
  name: get_linux_users
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8000/linux-users
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
