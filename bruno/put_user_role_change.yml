info:
  name: put_user_role_change
  type: http
  seq: 9

http:
  method: PUT
  url: http://localhost:8000/api/db/users
  body:
    type: json
    data: |-
      {
        "username": "tsahur",
        "roles_id": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
