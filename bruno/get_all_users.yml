info:
  name: get_all_users
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:8000/api/db/users
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
