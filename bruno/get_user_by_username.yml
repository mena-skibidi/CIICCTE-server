info:
  name: get_user_by_username
  type: http
  seq: 7

http:
  method: GET
  url: http://localhost:8000/api/db/users?username=skibidi
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
