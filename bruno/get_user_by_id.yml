info:
  name: get_user_by_id
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:8000/api/db/users?id=3
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
