info:
  name: command-test-execution
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8000/api/telemetry/linux-server-details
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
